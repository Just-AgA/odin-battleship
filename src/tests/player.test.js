const Player = require('../player');
const Gameboard = require('../gameboard');
const Ship = require('../ship');

describe('Player', () => {
  let player, enemy;

  beforeEach(() => {
    player = Player(false);
    enemy = Player(false);
  });
});
